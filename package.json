{
  "name": "partial-diff",
  "displayName": "Partial Diff",
  "description": "Compare a part of document to another",
  "version": "0.0.1",
  "publisher": "ryu1kn",
  "engines": {
    "vscode": "^1.0.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:extension.markSection1"
  ],
  "main": "./extension",
  "contributes": {
    "commands": [
      {
        "command": "extension.markSection1",
        "title": "Mark the selected text as 1st section to compare"
      },
      {
        "command": "extension.markSection2AndTakeDiff",
        "title": "Mark the selected text as 2nd section to compare and take diff"
      }
    ]
  },
  "scripts": {
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test-mode": "mocha test --recursive --ui tdd --watch"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.5.3",
    "sinon": "^1.17.4",
    "sinon-chai": "^2.8.0",
    "vscode": "^0.11.0"
  },
  "dependencies": {
    "temp": "^0.8.3"
  }
}
